---
# Choose the ENV to be tested (QA, DEV, STG, INT, PROD)
TestEnvironment: DEV

# Run tests in (localDevices, remote)
RunTestsIn: localDevices

Appium_JS_Path:

# Select device interested to test (Android, iOS)
DEVICE: Android

